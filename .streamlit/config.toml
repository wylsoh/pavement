[theme]
# 主色调：科技蓝
primaryColor="#00a8ff"

# 背景色：纯白，符合纸质印刷感
backgroundColor = "#ffffff"

# 侧边栏/次要背景色：极浅的灰色，区分层级但不喧宾夺主
secondaryBackgroundColor = "#f2f2f2"

# 文本颜色：深灰/纯黑，提高对比度
textColor = "#2f3640"

# 字体：强化学术感的衬线字体 (Serif)
font = "sans serif"